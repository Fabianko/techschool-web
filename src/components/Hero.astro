---
import { Icon } from 'astro-icon/components';

---

<header class="container stack center">
  <section class="hero stack center">
    <Icon name="logo" size={162} />
    <h1>TechSchool</h1>

    <h2>Primer evento tecnológico <span>inter regional</span></h2>

    <p>
      Participa en talleres presenciales donde aprenderás de expertos, ampliarás
      tu red y descubrirás las últimas tendencias
    </p>
  </section>

</header>

<style>
  .container {
    color: var(--white);
    max-width: 100vw;
    min-height: 100vh;
    position: relative;
    padding-block-start: 6.75rem;

    & .hero {
      z-index: 1;
      text-align: center;
      margin-block-start: -4.5rem;

      @media (width <= 768px) {
        margin-block-start: -3.5rem;
      }

      & h1 {
        font-size: 3.5rem;
        font-weight: 800;
        padding-block-start: 2.75rem;

        @media (width <= 768px) {
          font-size: 3rem;
        }
      }

      & h2 {
        font-size: 3rem;
        font-weight: 700;
        line-height: 48px;
        padding-block-start: 4.5rem;

        & span {
          color: var(--teal);
        }

        @media (width <= 768px) {
          font-size: 2rem;
        }
      }

      & p {
        width: 60%;
        font-weight: 400;
        font-size: 1.5rem;
        padding-block-start: 1.5rem;

        @media (width <= 768px) {
          width: 100%;
          font-size: 1rem;
        }
      }
    }

    &::after,
    &::before {
      content: '';
      z-index: -1;
      position: absolute;
      width: 60vw;
      aspect-ratio: 1 / 1;

      @media (width <= 1024px) {
        width: 24rem;
      }

      @media (width <= 768px) {
        width: 30rem;
      }
    }

    &::before {
      inset-block-start: -25rem;
      inset-inline-start: -9.75rem;
      background: radial-gradient(50% 50% at 50% 50%, var(--deep-blue) 0%, transparent 100%);

      @media (width <= 1024px) {
        inset-block-start: -12rem;
        inset-inline-start: -10rem;
      }

      @media (width <= 768px) {
        inset-block-start: -6rem;
        inset-inline-start: -12rem;
      }
    }

    &::after {
      inset-block-end: -22rem;
      inset-inline-end: -19.5rem;
      background: radial-gradient(50% 50% at 50% 50%, rgb(from var(--purple) r g b / 0.5) 0%, transparent 100%);

      @media (width <= 1024px) {
        inset-block-end: -12rem;
        inset-inline-end: -15rem;
      }

      @media (width <= 768px) {
        inset-block-end: -6rem;
        inset-inline-end: -15rem;
      }
    }
  }
</style>
